# Production environment variables for Vercel build
# Vite replaces import.meta.env.* at BUILD TIME, so this file must be committed.
# Since frontend and backend share the same Vercel domain, /api is the correct base URL.
VITE_API_URL=/api
